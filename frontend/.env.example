# Frontend Configuration

VITE_API_URL=http://localhost:5132/api

# Google OAuth Web client ID (from https://console.cloud.google.com/apis/credentials).
# Without this set, the Google sign-in button is hidden and you can use the
# "local dev login" box on the login page instead (development mode only).
VITE_GOOGLE_CLIENT_ID=813992795187-5i2pc7hnbr12rbum9j25ma7p5l4ou999.apps.googleusercontent.com
